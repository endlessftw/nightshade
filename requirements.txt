pytz>=2021.1
# On some systems (Windows), you may also need tzdata for zoneinfo
tzdata
discord.py>=2.3.0
python-dotenv
aiohttp
Pillow
# Voice support (PyNaCl) and optional streaming support
pynacl
yt-dlp
# Database support
asyncpg>=0.29.0
aiosqlite>=0.19.0
# Note: ffmpeg is a system binary and must be installed separately and available on PATH.
